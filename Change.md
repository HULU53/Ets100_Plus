# 改动说明

本文记录对话中对项目完成的全部改动，内容使用通俗语言说明。

## 一、应用名称和版本

- 应用名称由 `Fe` 改为 `E+`。
- 首页、读取页、设置页、“关于”弹窗和自动化通知均同步显示 `E+`。
- Android 包名由 `com.shuaiqiu.fuckets100` 改为 `com.hulu.etsplus`。
- 主代码、单元测试和仪器测试目录已迁移到新包名对应的目录。
- Manifest、测试断言、自动化配置和内部 Action 常量中的包名均已同步修改。
- 最终版本设置为：
  - `versionCode = 1`
  - `versionName = "1.0.0"`

注意：本次改变后，新版本会被 Android 视为一个全新的应用。旧版本的数据、权限和 Shizuku 授权不会自动迁移。

## 二、更新功能调整

- 移除了应用启动时的版本检测逻辑。
- 移除了本地版本与远程版本之间的比较。
- 移除了普通更新和强制更新弹窗。
- 移除了“立即更新”、更新下载地址和相关状态流。
- 删除了 `UpdateDialog.kt`。
- 删除了公告页中的“检测更新”按钮。
- 远程配置和发布脚本不再生成或保留旧更新字段。
- 保留远程公告、更新日志和 KillSwitch 功能。

## 三、防倒卖验证移除

- 移除了启动时的防倒卖验证码检查和拦截。
- 删除了验证码弹窗相关状态和界面。
- 删除了 `VerificationDialog.kt`。
- 删除了本地验证码的保存和读取方法。
- 删除了用于获取远程验证码的 `config/get_verification_code.py`。
- 远程配置中的验证码字段会在后续同步时被清理。

## 四、捐赠入口移除

- 删除首页中的“捐赠支持”入口。
- 删除设置页“关于作者”区域中的捐赠入口。
- 删除独立捐赠页面 `DonateActivity.kt`。
- 删除捐赠页面对应的 Opaque 和 KernelSU Classic 页面变体。
- 删除 Manifest 中的捐赠 Activity 注册。
- 删除应用内支付宝和微信捐赠二维码图片。
- 删除远程配置和发布脚本中的 `donateEnabled` 字段。

## 五、设置页和关于信息调整

- 删除“访问官网”选项。
- 删除“开源地址”选项。
- 删除“开源协议: GPL 3.0”说明。
- “关于 Fe”已改为“关于 E+”。
- 作者信息已改为 `Hulu53`。
- 删除作者头像及其图片资源。
- 将设置页中的“测试功能”改名为“悬浮窗”。
- 将对应简介改为“修改悬浮窗参数”。

## 六、其他名称同步

- 自动化悬浮窗通知标题改为 `E+ 自动化悬浮窗`。
- 分享签名改为 `E+守护你的答案喵~`。
- 分享图片文件名前缀改为 `EPlus_Answer_`。
- 分享图片保存目录和提示文字改为 `E+`。

## 七、保留的外部包名

以下包名属于外部应用或工具，不能修改，否则会导致功能失效：

- `com.ets100.secondary`
- `moe.shizuku.privileged.api`

## 八、编译和打包验证

- `:app:compileDebugKotlin` 编译成功。
- `:app:assembleDebug` 打包成功。
- 合并后的 APK 包名为 `com.hulu.etsplus`。
- 未发现旧包名残留。
- 当前只有项目原有的弃用提示和类型转换警告，没有编译错误。

## 九、文档记录

- 已创建并持续维护 `Codex.md`，用于记录实施过程和验证结果。
- 已创建本文件 `Change.md`，用于集中说明本次全部改动。

## 十、自动化答题基础框架

- 新增一套独立的自动化答题框架，原来的本地/云端答案读取流程保持不变。
- 入口位于设置页的“悬浮窗/测试功能”，可进入“测试功能1”页面。
- 测试功能1页面可以查看悬浮窗权限、无障碍权限、屏幕捕获授权状态。
- 页面内可以调整悬浮窗宽高、透明度、贴边间距、默认停靠方向和运行时拖动锁定。
- 这些悬浮窗设置会保存在 SharedPreferences 中，重启应用后仍然有效。
- 新增自动化任务选择页面，可以从已经读取成功的本地或云端试卷中选择题目。
- 每道题可以选择本次要使用的答案版本，然后生成自动化任务队列。
- 没有答案的题目不会进入任务队列。

## 十一、任务队列与会话控制

- `AutomationController` 负责管理任务列表、当前题目序号、运行状态和错误信息。
- 支持准备任务、开始、停止、上一题、下一题、等待开始、失败和清空。
- `AutomationOrchestrator` 负责真正执行任务。
- 每个选择题任务只执行一次“截图 -> OCR/节点读取 -> 选项定位 -> 点击”。
- 每个朗读任务会播放用户选中的答案。
- 当前任务成功后会自动切换到下一题，但不会自动开始下一题。
- 下一题会进入“已就绪”状态，需要用户再次点击悬浮窗里的“开始”。
- 最后一题完成后会停止任务队列。
- 任务失败时进入错误状态，不会继续盲目点击后续题目。

## 十二、自动化悬浮窗

- 新增 `AutomationOverlayService` 前台服务。
- 悬浮窗使用 Android Overlay 显示，可以拖动并自动吸附到屏幕左侧或右侧。
- 悬浮窗不会把触摸事件穿透到目标应用，自身区域会正常接收操作。
- 悬浮窗中可以查看当前题目、题目序号、任务进度和运行状态。
- 悬浮窗提供开始、停止、上一题和下一题按钮。
- 停止按钮会立即取消当前任务和正在播放的朗读。
- 悬浮窗参数继续使用测试页面中的配置。

## 十三、无障碍、页面读取与 OCR

- 新增 `AutomationAccessibilityService` 无障碍服务。
- 无障碍服务可以读取当前页面的可见文字和屏幕坐标。
- 无障碍节点会忽略悬浮窗自身覆盖的区域。
- Android 11 及以上优先使用无障碍截图。
- 无障碍截图不可用时，可以降级使用 MediaProjection。
- 截图会把悬浮窗区域涂黑，避免 OCR 把悬浮窗文字误认为题目内容。
- 新增 ML Kit 中文 OCR，支持返回文字、位置和匹配信息。
- 识别时优先使用无障碍节点，节点文字不足时才使用 OCR。

## 十四、选项定位与点击

- 新增 `OptionLocator`，根据答案中的选项标签或选项正文查找页面位置。
- 点击前会先检查当前页面题目是否与任务题目匹配。
- 题目不匹配时会中止点击，并记录日志，不会继续使用错误坐标。
- 会自动过滤屏幕外节点和无效坐标。
- 匹配分数低于阈值时不会点击。
- 点击顺序为：
  1. 先查找目标坐标处最小、可用的无障碍节点并执行点击。
  2. 节点点击失败时再派发坐标手势。
  3. 无障碍点击不可用时，才降级到 Shizuku 或 Root 的 `input tap`。
- 坐标手势只有在系统回调完成后才算成功。
- 所有点击入口都会检查坐标，避免负数坐标导致崩溃。
- 无障碍服务配置增加了 `canPerformGestures`。
- 设备更新配置后，可能需要关闭再重新打开无障碍服务。

## 十五、朗读与音频

- 新增系统 TextToSpeech 朗读，默认使用中文。
- 朗读完成后会自动切换到下一题，并停在等待开始状态。
- 停止按钮可以立即停止当前朗读。
- 新增可选的音频增强设置。
- 开启音频增强后，会尝试保存原媒体音量、扬声器状态和音频模式。
- 可通过 Shizuku 或 Root 尝试提高媒体音量并保持扬声器播放。
- 朗读结束后会恢复原来的音频状态。
- 当前使用的是系统 TTS，不是在线 AI 语音。
- 目前没有预生成 PCM/WAV，也没有虚拟麦克风。

## 十六、屏幕捕获与性能调整

- 无障碍截图失败时，MediaProjection 作为备用截图方案。
- MediaProjection 不再持续转换每一帧。
- 未被任务请求的帧会直接关闭，不创建 Bitmap。
- 每次选择题任务只请求一次截图。
- 实际转换后的 Bitmap 最多每秒生成一张，即最高 1 FPS。
- 单次截图等待超时为 1500 毫秒。
- 同一时间只允许一个截图请求，避免重复等待和资源堆积。
- 该调整减少了大量全分辨率 Bitmap 创建、内存压力和文件写入。

## 十七、日志系统

- 新增 `AutomationLog`，自动化模块会写入 Logcat 和内存日志。
- Logcat 标签为 `FeAutomation`。
- 内存日志最多保留 300 条。
- 测试功能1页面可以查看最近 12 条日志，并支持清空。
- 日志会记录任务、会话、当前题号、任务类型、坐标、匹配分数和失败原因。
- 为避免泄露，日志不会保存完整答案和完整 OCR 文本。
- TTS 只记录文字长度，任务标题和命令输出会限制长度。

## 十八、文件日志和崩溃日志

- 自动化日志会同步写入 `/storage/emulated/0/Download/EplusDebug.txt`。
- 最新日志始终前插到文件顶部。
- 文件最多保留约 100 万字符，超出后删除最旧内容。
- 日志行包含时间戳、级别、模块、会话 ID、任务序号、任务 ID、任务类型和任务标题。
- 新增 `AutomationCrashHandler`，应用启动时安装全局未捕获异常处理器。
- Java/Kotlin 未捕获异常会记录完整堆栈、线程、设备信息、版本和当前任务。
- `OutOfMemoryError` 等 JVM Error 也可以记录。
- 崩溃日志会同步写入，避免进程退出时来不及保存。
- 不支持记录 ANR、Native crash、SIGSEGV、LowMemoryKiller 等系统级崩溃。

## 十九、日志开关

- 在应用主设置页“其他选项”的最后增加“自动化日志”开关。
- 默认开启。
- 关闭后会停止写入 Logcat、内存日志、`EplusDebug.txt` 和新的崩溃堆栈。
- 关闭时会清空当前任务上下文。
- 测试功能1中的手动“清空”不受开关影响。
- 开关状态保存在 `automation_logging_enabled` 中。

## 二十、主要问题修复

### 20.1 不可变 Bitmap 崩溃

- 无障碍截图复制出的 Bitmap 曾被标记为不可变。
- 对不可变 Bitmap 创建 `Canvas` 会抛出 `IllegalStateException`。
- 已改为复制可变 Bitmap，并在遮罩函数中增加防御处理。

### 20.2 点击返回成功但目标没有响应

- 旧实现把 `dispatchGesture()` 返回 true 错当成点击成功。
- 新实现优先使用无障碍节点点击。
- 手势必须等到完成回调才算成功。
- 点击操作已经切换到主线程执行。

### 20.3 题目不匹配导致负坐标崩溃

- 当前页面和答案题目不一致时，旧版本可能匹配到屏幕外节点。
- 屏幕外节点产生了负数坐标。
- 构造手势时会抛出 `Path bounds must not be negative`。
- 新版本会先校验题目、过滤屏幕外节点、验证坐标，并保护手势构造。
- 题目不匹配现在只会中止和记录日志，不会崩溃。

### 20.4 任务完成后自动开始下一题

- 已改为完成当前任务后自动切题，但保持“已就绪”。
- 用户必须再次点击“开始”才会执行下一题。

## 二十一、权限和运行条件

- 应用需要悬浮窗权限。
- 无障碍服务需要用户手动在系统设置中启用。
- MediaProjection 需要用户手动授权屏幕捕获。
- Shizuku 和 Root 是可选能力，用于点击降级和音频增强。
- 新增前台服务、媒体投影前台服务、通知和特殊用途前台服务权限。
- 包名改为 `com.hulu.etsplus` 后，旧版本的数据和授权不会自动迁移。

## 二十二、当前限制

- 没有多帧 OCR 稳定性检测。
- 没有点击后的页面变化验证。
- 没有答题结果的自动回传或正确率检查。
- 没有在错误点击前弹出二次确认。
- 没有检测目标应用是否真正进入录音状态。
- 没有处理蓝牙音频路由。
- 系统 TTS 是否能被目标应用录音，取决于回声消除、降噪和设备音频策略。
- Root/Shizuku 音频命令兼容性取决于设备和 Android 版本。
- MediaProjection 会话目前不能持久恢复。

## 二十三、验证状态

- `:app:compileDebugKotlin` 编译通过。
- `:app:testDebugUnitTest` 单元测试通过。
- `git diff --check` 检查通过。
- 原有自动化功能仍需要在真机上逐步验证权限、点击坐标、OCR、悬浮窗和录音回采。
- 当前文档只记录已经完成的代码和静态验证，不代表所有设备端功能均已验证成功。

## 二十四、应用图标与新图标接入

- 将新的 `E +` SVG 图标转换为 Android 矢量资源。
- 新增完整图标 `ic_eplus.xml`、背景层、前景层和单色层。
- 普通应用图标和圆形应用图标均已切换到新图标。
- 旧图片资源不再作为新的自适应图标内容使用。
- 设置页“关于 E+”中的应用图标已改为新生成的图标。
- “关于 E+”图标使用圆角方形展示，保持原始图形比例。

## 二十五、首页布局重构

- 首页新增固定顶部图标栏，顶部栏只属于首页。
- 顶部栏居中的图标会使用主界面自定义色号，不显示版本号。
- 首页主内容区改为从上到下的三块结构。
- 第一块显示激活状态和无障碍权限状态。
- 第二块显示应用启动总次数和已读取文件数。
- 第三块是完整公告区域，包含标题和正文，并占据内容区下半部分。
- 公告区域整体可以点击，进入原有公告与更新详情页。
- 没有公告时显示“公告”和“暂无公告”。
- 首页不再显示更新日志，更新日志仍保留在详情页。
- 激活状态支持“以云端模式激活”“以云端模式未激活”和未选择模式时的“未激活”。
- 计数格式严格使用等号，例如“应用启动总次数=1”。
- 平板和宽屏限制最大内容宽度，避免界面被过度拉伸。
- 内容过高时允许区域内部滚动。
- 原首页顶部栏、旧状态大卡片和旧公告摘要卡片已经移除。
- 非激活区域使用低对比度卡片和细边框进行统一美化。

## 二十六、启动次数与文件读取统计

- 新增 `AppUsageStats`，负责应用级统计。
- 冷启动次数保存到应用私有目录的独立文件中。
- 首次打开时启动次数显示为 1。
- 应用退回后台再返回不会重复增加冷启动次数。
- 清除应用数据和重新安装后，启动次数会重置。
- Android 备份恢复后，启动次数会保留。
- 新增累计已读取文件数统计。
- 同一个文件只统计一次，重复读取不会重复增加。
- 数据文件、资源文件和云端文件都会统计。
- 目录和读取失败的文件不会统计。
- 本地 Shizuku、Root 和 Direct Read 的成功读取均已接入统计。
- 云端 `content.json` 成功读取后也会接入统计。
- 文件统计同样会在清除数据后重置，并在备份恢复后保留。

## 二十七、状态检测和设置入口

- 无障碍状态现在要求服务已经启用并且真正连接。
- 只勾选系统设置但没有连接成功时，不会显示“无障碍权限已激活”。
- 云端模式的激活条件增加文件访问、悬浮窗和应用列表权限。
- 设置页保留原有“运行授权”入口。
- 设置页新增“激活状态”入口，并指向同一个激活页面。

## 二十八、主题切换框架和主界面图标色号

- 新增 `UiStyle`，暂时包含 `Material` 和 `Miuix` 两个运行时选项。
- 新增 `LocalUiStyle`，为以后接入真实 Miuix 组件预留统一入口。
- 主题设置页新增“界面风格”切换区域。
- 当前 `Miuix` 选项主要切换卡片圆角等外观框架，尚未接入完整 Miuix 依赖。
- 主题设置页新增“主界面图标”色号设置。
- 色号支持输入标准六位十六进制颜色，例如 `#2196F3`。
- 提供“默认”按钮，可恢复原来的 `#2196F3`。
- 主界面图标颜色只影响首页顶部图标，不修改桌面应用图标。
- 浅色背景会自动使用深色图形，避免白色图形无法辨认。
- 主界面图标色号使用 `SharedPreferences` 保存。
- 从主题设置返回首页后，图标色号会自动刷新。
- `FeThemeWrapper` 已增加界面风格参数，并通过 `CompositionLocal` 向页面提供当前风格。

## 二十九、网站远程配置与公告接口

- App 正式配置源改为网站接口：
  - `https://hulu53.github.io/Eplus/api/app/config.json`
  - `https://hulu53.github.io/Eplus/api/app/config`
- 已移除不再属于当前维护者的 Gitee 和 GitHub 旧配置源。
- 默认更新日志地址改为网站接口：
  - `https://hulu53.github.io/Eplus/api/app/changelog.md`
- 公告正文继续使用网站纯文本接口：
  - `https://hulu53.github.io/Eplus/api/app/current.txt`
- 公告和更新日志继续使用 App 原生文本展示，不使用网页组件。
- 网站接口未发布前出现 404 属于预期现象，发布 GitHub Pages 后即可生效。

## 三十、分享水印调整

- 导出图片底部的分享文字已由旧官网版本改为：
  - `E+守护你的答案`
- 复制或导出纯文本时的同一落款也会同步变化。
- 本条内容覆盖前文中较早记录的分享签名字样。
- 水印字号、颜色和居中位置没有变化，不需要额外兼容处理。

## 三十一、本轮验证状态

- 首页重构和主题框架完成后，`:app:compileDebugKotlin` 与 `:app:assembleDebug` 均已通过。
- 图标和首页相关资源已成功打包进 Debug APK。
- `git diff --check` 已通过。
- 移除旧配置源和修改分享水印后，只完成了静态引用检查。
- 这两处调整属于字符串和地址列表修改。

## 三十二、构建工具链升级

- Gradle Wrapper 从 `9.3.1` 升级到 `9.7.1`。
- Android Gradle Plugin 从 `9.1.1` 升级到 `9.4.0`。
- Kotlin 从 `2.2.10` 升级到 `2.4.20`。
- Compose BOM 从 `2026.02.01` 升级到 `2026.09.00`。
- 实际解析到的 Compose UI 版本为 `1.12.1`。
- 用户级安装了 JDK 21，用于满足新版构建栈要求。
- Gradle 下载源切换为腾讯镜像，并提高下载超时。
- App 的 `minSdk` 从 26 提升到 33，与 Miuix blur 模块要求一致。

## 三十三、Miuix 依赖和根主题接入

- 加入 Miuix `0.9.4-rc01` 的核心 UI、图标、Preference、Navigation 和 Blur 模块。
- `FeThemeWrapper` 改为 Material 与 Miuix 根主题二选一，不再嵌套两套主题。
- 新增 `MiuixThemeBridge.kt`，把现有主题颜色映射到 Miuix `ThemeController`。
- Miuix 模式支持现有明暗模式和动态取色偏好。
- `LocalUiStyle` 继续作为全局界面风格入口。
- 新增 `LocalBlurEnabled`，用于全局提供模糊开关状态。

## 三十四、Miuix 页面迁移

- 首页新增完整 Miuix 实现，包括顶部图标栏、激活状态、统计信息和公告卡片。
- 设置页新增 Miuix 实现，使用 Miuix Card、ArrowPreference 和 SwitchPreference。
- 主题设置页新增 Miuix 实现，支持界面风格、色号、颜色主题、明暗模式和返回手势。
- 主界面底部导航增加 Miuix NavigationBar 分支。
- 读取页完成 Miuix 迁移。
- 新增 `ReadMiuixCompat.kt`，将读取页原有 Material 调用统一渲染为 Miuix 组件。
- 读取页业务逻辑、数据结构和导出图片功能保持不变。
- 读取页现在是统一 Miuix 渲染，没有保留独立的旧 Material 双实现。

## 三十五、Miuix 模糊效果

- 新增“界面模糊”开关，默认开启。
- 模糊设置使用 `blur_enabled` 持久化。
- Miuix 首页顶部栏、状态卡和公告卡支持背景模糊。
- Miuix 设置页顶部栏支持背景模糊。
- Miuix 主题设置页顶部栏支持背景模糊。
- 关闭开关后不执行模糊绘制，避免额外渲染开销。
- 模糊使用 Miuix 的 `rememberLayerBackdrop`、`layerBackdrop` 和 `textureBlur`。

## 三十六、近期文案调整

- Miuix 设置页中的“测试功能”改为“悬浮窗设置”。
- 对应简介改为“修改悬浮窗参数”。
- 该入口仍然打开原有 `TestFeature1Activity`，功能路径没有变化。

## 三十七、近期验证状态

- 构建工具链升级后，`:app:compileDebugKotlin` 编译通过。
- Miuix 根主题、首页、设置页、主题页、底部导航和读取页迁移后，`:app:assembleDebug` 打包通过。
- 模糊效果与开关接入后，`:app:compileDebugKotlin` 和 `:app:assembleDebug` 均通过。
- 当前没有 Android 设备或模拟器连接，因此尚未完成实机模糊效果和交互验证。
## 二十四、崩溃记录范围扩展

- 新增历史进程退出信息采集。
- 应用下次启动时，会自动读取 Android 保存的 Java 崩溃、Native crash、ANR、低内存和异常退出记录。
- 如果系统提供 trace，会把完整 trace 写入 `EplusDebug.txt`。
- 会记录退出原因、进程名、时间、设备信息和简单内存数据。
- 增加重复记录保护，避免每次启动都把旧崩溃重复写入。
- 协程中的未捕获异常也会进入统一崩溃日志。
- Native crash 发生时应用进程已经退出，无法当场写文件，只能在下次启动补录。
- ANR、Native crash 和系统强制终止仍然可能受系统日志保留时间限制。
- Kotlin 编译检查已通过。

## 三十八、答案详情页顶部安全区修复

- 修复 Miuix 风格下查看答案页面与系统状态栏重叠的问题。
- Miuix 顶部栏现在会预留 `WindowInsets.statusBars`。
- 返回按钮、试卷标题和右侧操作按钮不会再进入状态栏区域。
- 本次只修复安全区，不改变答案内容和页面结构。

## 三十九、答案页一键生成悬浮窗任务

- 答案详情页右上角新增“悬浮窗任务”图标按钮。
- 按钮位于原“更多操作”按钮左侧。
- 按钮使用悬浮窗相关图标，并带有无障碍说明。
- 点击后只处理当前正在查看的试卷，不会误选同名或同地区的其他试卷。
- 自动遍历当前试卷中的全部题目。
- 没有答案的题目会被跳过。
- 每道题默认选择第一个答案版本。
- 任务 ID、选择题/朗读任务类型和选项提取继续复用现有任务工厂。
- 如果当前答案任务正在运行，会先停止并隐藏旧悬浮窗。
- 新任务会替换旧队列，并进入“已就绪”状态。
- 生成任务后自动跳转到现有“设置-悬浮窗”页面。
- 不会自动显示悬浮窗，也不会自动开始第一题。
- 当前试卷没有可生成答案时，只显示提示，不修改旧队列，也不跳转。
- 手动“选择答案”与全选生成任务的流程保持不变。

## 四十、Miuix 模糊实验性调整

- Miuix 主题设置中的“界面模糊”已改名为“界面模糊（实验性）”。
- 此前设置页和主题设置页顶部栏的模糊会形成自引用图形层，触发 Android HWUI 原生栈溢出。
- 已移除设置页顶部栏的实际模糊。
- 已移除主题设置页顶部栏的实际模糊。
- 首页曾改为独立背景捕获层，尽量避免模糊层自引用。
- 由于首页没有可滚动内容，背景变化有限，最终决定暂时搁置模糊效果继续调优。
- 模糊开关和实验性入口继续保留。
- 本条内容更新并覆盖前文关于设置页和主题页也启用模糊的描述。

## 四十一、答案页图形层稳定性优化

- 读取页兼容渲染层中的 `Surface` 改为普通裁剪背景容器，减少图形播放层。
- Miuix 模式下 `FeOutlinedCard` 也改为普通背景容器。
- 保留原有圆角、背景色和点击行为。
- 目标是降低答案页 RenderNode 嵌套深度，避免 HWUI 栈溢出。
- 业务数据、答案解析和导出图片逻辑没有变化。

## 四十二、状态

- 答案页安全区修复后，`:app:compileDebugKotlin` 和 `:app:assembleDebug` 均通过。
- 答案页一键生成悬浮窗任务后，`:app:compileDebugKotlin` 和 `:app:assembleDebug` 均通过。
- Miuix 模糊实验性调整和答案页图形层优化后，编译与打包均通过。

## 四十三、日志文件路径调整

- 自动化日志文件名由 `fe_test.txt` 改为 `EplusDebug.txt`。
- 新路径为 `/sdcard/Download/EplusDebug.txt`。
- 旧文件不会被自动删除，只是不再继续写入。
- Kotlin 编译检查通过。
